version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
      - MONGO_URI=mongodb://localhost:27017/deployease
    volumes:
      - ./backend/deployments.json:/app/deployments.json

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
